<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rpl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_notes_empty"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_notes_empty_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity"/>
        <activity android:name=".ui.Notes.NotesActivity"/>
        <activity android:name=".ui.Plus.PlusActivity"/>
        <activity android:name=".ui.SignUp.SignUpActivity"/>
        <activity
            android:name=".ui.HomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.ComposeMailActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.ViewMailActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>